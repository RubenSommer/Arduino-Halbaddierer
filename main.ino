void setup() {
    /*set pinmode of GPIO pins */
    pinMode(2, OUTPUT);
    pinMode(3, OUTPUT);
    pinMode(4, INPUT);
    pinMode(5, INPUT);
    /* set baud rate*/
    Serial.begin(9600);
}

void loop() {
    
}